VPC:
- VPC is a regional level
-  we can create upto 5 vpc's per region
- 
NAT GATEWAY:
- Nat Gateway allows only one direction egress, no inegress

- There are 3 levels of security
  - NACL - Subnet level --> Netwoek Access Control List
      - by default blocks both inbound and outbound rules
      - if enables inbound traffic and need to enable outbound traffic 
  - SG - EC2 level  --> Security Group
      - by default allows outbound traffics and allow inbound as per rules enable by developer.
      - no need to enable outbound rule in by default allows to all traffic.
  - WAF - Application level  --> Web Application Firewall


- SG 